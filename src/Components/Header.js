import React from 'react';
import { Jumbotron } from 'react-bootstrap';

const header = () => {
	return (
		<Jumbotron fluid className='jumbo'style={{ height: 200, marginBottom: 0, backgroundColor: '#FFFFFF' }}>
		</Jumbotron>
	);
};

export default header;
